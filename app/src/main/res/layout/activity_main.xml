<?xml version="1.0" encoding="utf-8"?>

<!--
    XLumen main activity layout.

    Intentionally minimal - most users never open this after initial setup.
    Real interaction is via the Quick Settings tile.

    Views:
        txt_a11y_status  - warns user if accessibility service is not enabled
        txt_status       - current service state (running / stopped)
        btn_settings     - opens SettingsActivity

     XLumen main activity layout.
         Wrapped in ScrollView because warning text can exceed one screenful.
         This is intentional - first time users need to read everything.
-->

<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

    <TextView
        android:id="@+id/txt_a11y_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:paddingBottom="16dp"
        android:text="Checking accessibility service..." />

    <TextView
        android:id="@+id/txt_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:paddingBottom="24dp"
        android:text="XLumen stopped." />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="12dp">

            <Button
                android:id="@+id/btn_start"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="4dp"
                android:text="Start" />

            <Button
                android:id="@+id/btn_stop"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:enabled="false"
                android:text="Stop" />

        </LinearLayout>

    <Button
        android:id="@+id/btn_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Settings" />

    <TextView
        android:id="@+id/txt_a11y_warning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:paddingBottom="8dp"
        android:text="IMPORTANT: When enabling XLumen in Accessibility Settings,
Android will show warnings stating this app can observe your screen.
These warnings are correct and expected.  XLumen reads screen brightness
to protect your eyes.  It does not record, store, or transmit anything.
Google's warnings will likely become more alarming in future Android versions.
This is intentional on Google's part.  You have been warned." />

    <!-- Opens top-level Accessibility Settings.
         Safer choice - less likely to trigger Google version-specific snafus.
         Recommended for first-time users. -->
    <Button
        android:id="@+id/btn_open_a11y_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Open Accessibility Settings"
        android:layout_marginBottom="4dp" />

    <!-- Opens XLumen-specific A11y page directly.
         Convenience shortcut - may behave differently across Android versions.
         Google has broken this deep link before and may again. -->
    <Button
        android:id="@+id/btn_open_a11y_xlumen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Open XLumen Accessibility Page (may vary by Android version)"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/txt_debug"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:paddingTop="24dp"
        android:text="" />

    <Button
        android:id="@+id/btn_read_debug"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Read Debug Log" />

    </LinearLayout>

</ScrollView>
